# Nix build outputs (nix build / nixos-rebuild build)
result
result-*

# Stray build/eval logs
*.log

# Claude Code local settings (machine-specific, not shared)
.claude/settings.local.json
**/.claude/settings.local.json

# Claude Code agent worktrees (throwaway copies of the tree)
.claude/worktrees/

# Fleet reference notes for AI sessions (local working notes, not repo docs).
# Drop this line if they're ever worth tracking.
ai-docs/

# Editor / OS noise
*.swp
.direnv/
.DS_Store
